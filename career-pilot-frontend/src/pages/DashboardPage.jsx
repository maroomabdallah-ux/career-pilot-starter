import { ArrowRight, BriefcaseBusiness, Check, Compass, FileText, FolderKanban, GraduationCap, Sparkles, Target, Wrench } from "lucide-react";
import { Link } from "react-router-dom";
import AsyncState from "../components/common/AsyncState";
import { useCareerData } from "../hooks/useCareerData";
import { calculateProfileCompletion } from "../services/profileCompletion";

function StrengthRing({score}){return <div className="command-ring" style={{'--score':score}}><div><strong>{score}%</strong><span>Profile strength</span></div></div>}
export default function DashboardPage(){const {user,profile}=useCareerData();const p=profile.data;const completion=calculateProfileCompletion(p);const data=[['Education',p?.education?.length,GraduationCap],['Experience',p?.experiences?.length,BriefcaseBusiness],['Projects',p?.projects?.length,FolderKanban],['Skills',p?.skills?.length,Wrench]];return <main className="page command-center"><section className="command-hero"><div><span className="section-eyebrow">Career Command Center</span><h1>Good to see you, {user.data?.first_name}.</h1><p>Your career profile is <strong>{completion.score}% ready</strong>. {completion.score<100?'One focused step can make it stronger.':'Your foundation is ready for what comes next.'}</p><div><Link className="button primary" to={completion.nextAction.to}>{completion.nextAction.label}<ArrowRight size={16}/></Link><button className="button ai-command" disabled><Sparkles size={16}/> Ask CareerPilot AI <small>Coming soon</small></button></div></div><div className="command-status"><span>Next best action</span><strong>{completion.nextAction.label}</strong><small>Calculated from your real profile data</small></div></section><AsyncState loading={profile.isLoading} error={profile.error?.response?.status===404?null:profile.error}><section className="momentum-module"><div className="momentum-copy"><span className="section-eyebrow">Career momentum</span><h2>Your professional foundation, connected.</h2><p>CareerPilot evaluates meaningful profile evidence—not empty fields—to guide your next move.</p><Link to="/app/profile" className="text-button">Open Profile Blueprint <ArrowRight size={14}/></Link></div><StrengthRing score={completion.score}/><div className="momentum-route">{data.map(([name,count,Icon],i)=><Link to={`/app/profile/${name.toLowerCase()}`} key={name} className={count?'ready':''}><span><Icon size={17}/></span><div><small>0{i+1}</small><strong>{name}</strong><em>{count||0} added</em></div>{i<data.length-1&&<i/>}</Link>)}</div></section><section className="command-bento"><article className="resume-readiness"><div><span className="section-eyebrow">Resume readiness</span><h2>{completion.score>=70?'Ready to shape your first resume':'Build the evidence behind your resume'}</h2><p>Resume Studio will use the structured profile data you choose to provide.</p></div><div className="readiness-list">{data.map(([name,count])=><span className={count?'ready':''} key={name}>{count?<Check size={14}/>:<i/>}{name}</span>)}</div><Link className="button secondary" to="/app/resume">Preview Resume Studio</Link></article><article className="career-direction"><Target size={22}/><span className="section-eyebrow">Career direction</span><h2>{p?.target_roles?.[0]||'Choose your target roles'}</h2><p>{p?.target_roles?.slice(1,3).join(' · ')||'Add direction to make future guidance more relevant.'}</p><Link to="/app/profile" className="text-button">Refine direction <ArrowRight size={14}/></Link></article><article className="matching-preview"><Compass size={23}/><div><span className="section-eyebrow">Global opportunities</span><h2>Job matching</h2><p>{completion.score>=70?'Your profile foundation is nearly ready for the future matching phase.':'Complete your profile before intelligent matching becomes available.'}</p></div><span className="coming-label">AI phase · Coming soon</span></article><article className="activity-summary"><span className="section-eyebrow">Your profile now contains</span><div>{data.map(([name,count,Icon])=><span key={name}><Icon size={15}/><strong>{count||0}</strong>{name}</span>)}</div><small>Live profile data · No fabricated activity</small></article></section></AsyncState></main>}
