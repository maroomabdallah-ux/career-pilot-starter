services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: career_pilot
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5440:5432"
    volumes:
      - career_pilot_pgdata:/var/lib/postgresql/data
volumes:
  career_pilot_pgdata:
