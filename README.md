# CareerPilot AI Starter

Two independent projects:
- `career-pilot-backend` — Python + FastAPI + LangGraph + MCP
- `career-pilot-frontend` — React JS + Vite

Recommended: create two separate Git repositories from these folders.
# career-pilot-starter
